# Copie este ficheiro para .env (e opcionalmente .env.local para overrides) e preencha.
# NUNCA comite .env/.env.local com valores reais (já estão no .gitignore).

VITE_BASE44_APP_ID=
VITE_BASE44_APP_BASE_URL=
VITE_BASE44_FUNCTIONS_VERSION=
